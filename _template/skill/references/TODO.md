# TODO reference title

Put the real Unity standard / template / cheat-sheet the skill enforces here — the detail
that would bloat SKILL.md. The skill loads this on demand (progressive disclosure).

Rename this file to match its topic (e.g. `flow-standards.md`, `deck-templates.md`).
